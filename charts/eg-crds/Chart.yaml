apiVersion: v2
name: eg-crds
description: Gateway API and Envoy Gateway CRDs -- install once per cluster.
type: application
# version is injected at release time: helm package --version $RELEASE_VERSION
# Do not bump this manually.
version: 0.0.0-dev
# appVersion tracks the bundled EG version, also injected at release time.
appVersion: "0.0.0-dev"
