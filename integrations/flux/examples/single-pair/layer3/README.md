# Layer 3 resources

These are operator-managed. Apply AFTER the HelmRelease (Layer 2) is Ready.

Delete these BEFORE deleting the HelmRelease -- see teardown ordering in
integrations/README.md.

For reference manifests see examples/single-tier/ in the repo root.
